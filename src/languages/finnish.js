/// #### finnish

import * as quotes from '../quotes';

export default {
    quotes: [[quotes.DOUBLE_CLOSE_UP, quotes.DOUBLE_CLOSE_UP], [quotes.SINGLE_CLOSE_UP, quotes.SINGLE_CLOSE_UP]],
    dash: 'en',
    rules: []
};

export function tests() {
    return [];
}
